# FileShort

Free-first file utility website.

## Current MVP
- Image compression (browser-side)
- Basic PDF optimization (browser-side)
- Responsive homepage

## Deploy
Cloudflare Workers Static Assets serves the `public/` directory using `wrangler.jsonc`.
